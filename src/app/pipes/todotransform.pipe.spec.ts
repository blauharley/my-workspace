import { TodotransformPipe } from './todotransform.pipe';

describe('TodotransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TodotransformPipe();
    expect(pipe).toBeTruthy();
  });
});
